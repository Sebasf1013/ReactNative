// Mostrar en consola el siguiente dibujo utilizando la estructura whilen (desde 1 * hasta 14*)
let numero = 1; 
let dibujo = ""; 
while (numero < 15){
    dibujo += "*"; 
    console.log(dibujo); 
    numero++;
}